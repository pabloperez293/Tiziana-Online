 <!-- ========================= -->
  <!-- MYSQL / SQL -->
  <!-- ========================= -->

  <!--

  CREATE DATABASE tiziana;

  USE tiziana;

  CREATE TABLE productos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(255),
    categoria VARCHAR(255),
    precio_minorista DECIMAL(10,2),
    precio_mayorista DECIMAL(10,2),
    imagen TEXT,
    destacado BOOLEAN DEFAULT FALSE
  );

  -->
